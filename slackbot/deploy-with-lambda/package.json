{
  "name": "whos-out-slackbot-lambdas",
  "version": "1.1.0",
  "description": "Call BambooHR's API to find out who is currently Out Of Office. Group and filter on employee information such as location or department. Results are returned matching the current date that the code is run.",
  "main": "index.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "serverless": "^3.25.1",
    "serverless-pseudo-parameters": "^2.6.1"
  }
}
